<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mady.dynamicds.mapper.MoredataMapper">

    <select id="getAllUser1" resultType="com.mady.dynamicds.model.User">
        select * from t_user
    </select>

    <select id="getAllUser2" resultType="com.mady.dynamicds.model.User">
        select * from t_user
    </select>


    <insert id="addUserGetID1" parameterType="com.mady.dynamicds.model.User" >
        insert  into t_user values (#{id} , #{name} ,#{createTime})
    </insert>

    <insert id="addUserGetID2" parameterType="com.mady.dynamicds.model.User" >
        insert  into t_user values (#{id} , #{name} ,#{createTime})
    </insert>

</mapper>